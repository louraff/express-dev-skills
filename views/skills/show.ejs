<!DOCTYPE html>
<html>
  <head>
    <title>Skills</title>
    <link rel="stylesheet" href="stylesheet.css" />
  </head>
  <body>
    <main>
      <nav>
        <ul>
          <li><a href="/skills">&lt; Back</a></li>
        </ul>
      </nav>

      <h1><%= skill.name %></h1>
      <h2>
        <strong>Expertise: <%= skill.expertise %></strong> -
        <small>Years of Experience: <%= skill.yearsExperience %></small>
      </h2>

      <h6>Is currently using? <%= skill.currentlyUsing %></h6>
      <form
        action="/skills/<%= skill.id %>?_method=DELETE"
        method="POST"
        class="delete-form"
      >
        <button type="submit">Delete</button>
      </form>
    </main>
  </body>
</html>
